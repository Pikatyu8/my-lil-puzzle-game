# my-lil-puzzle-game

# 🧩 Гайд по дизайну уровней (Grid Puzzle)

Уровни хранятся в файле `levels.json`. Это список JSON-объектов.
Игра поддерживает два типа уровней, но основной и самый мощный — это **Condition** (условия).

## 1. Базовая структура уровня

Каждый уровень — это объект с обязательными полями:

```json
{
  "name": "Название уровня",
  "type": "condition",       
  "grid": [16, 12],          // [Ширина, Высота] в клетках
  "start": [0, 0],           // [X, Y] стартовая позиция (с 0)
  "hint": "Текст подсказки", // Опционально
  "conditions": [ ... ],     // Список условий победы
  "walls": [ ... ],          // Зеленые стены (блокируют путь)
  "poison": [ ... ]          // Красные барьеры (смерть)
}
```

---

## 2. Координаты и Макросы

Координаты начинаются с `0, 0` (верхний левый угол).
`X` — вправо, `Y` — вниз.

В полях, где требуются списки клеток (например, `cells`), можно использовать **макросы** вместо координат:
*   `"center"` — центральная клетка.
*   `"corners"` — 4 угловые клетки.
*   `"edges"` — все клетки по периметру поля.

---

## 3. Условия победы (`conditions`)

Это "мозги" уровня. Чтобы выиграть, игрок должен выполнить **ВСЕ** условия из списка одновременно.

### А. `end_at` (Финиш)
Игрок должен закончить свой ход в одной из указанных клеток.
```json
{
  "check": "end_at",
  "cells": [[15, 11]]  // Или макрос "corners"
}
```

### Б. `total_steps` (Счетчик шагов)
Проверяет длину пути.
*   **operator**: `==` (ровно), `>` (больше), `<` (меньше), `>=` (не менее), `<=` (не более).
```json
{
  "check": "total_steps",
  "count": 10,
  "operator": "=="
}
```

### В. `visit_cells` (Контрольные точки)
Требует, чтобы игрок посетил (наступил) на определенные клетки в любой момент игры.
*   **match**: `"any"` (хотя бы одну из списка) или `"all"` (обязательно все из списка).
```json
{
  "check": "visit_cells",
  "cells": [[5, 5], [6, 6]],
  "match": "all" 
}
```

### Г. `cell_has_steps` (Тайминг и Порядок)
Самое мощное условие. Проверяет, **на каком именно шаге** игрок наступил в клетку.
*   **required_steps**: Список номеров шагов (0 — это старт).
*   **match**: `"any"` (совпал любой шаг из списка) или `"all"` (клетка должна быть посещена на всех указанных шагах, т.е. возврат в неё).

*Пример: Игрок должен быть в клетке [3,3] ровно на 5-ом ходу.*
```json
{
  "check": "cell_has_steps",
  "cells": [[3, 3]],
  "required_steps": [5],
  "match": "any"
}
```

---

## 4. Барьеры (Walls и Poison)

Барьеры рисуются между клетками.
Формат записи: `[[x, y], "side", "type"]`.

*   **`[x, y]`**: Координата клетки, к которой "привязан" барьер.
*   **`"side"`**: Сторона клетки — `"up"`, `"down"`, `"left"`, `"right"`.
*   **`"type"`**:
    *   `"inner"` — **Ловушка**. Нельзя выйти ИЗ этой клетки через эту сторону. (Снаружи войти можно).
    *   `"outer"` — **Броня**. Нельзя войти В эту клетку через эту сторону. (Изнутри выйти можно).
    *   `"both"` — **Стена**. Полная блокада (толстая линия).

### 🟢 Walls (Стены)
Просто не пускают игрока. Игрок теряет ход (шаг не засчитывается), но игра продолжается.
```json
"walls": [
  [[4, 4], "right", "both"],   // Обычная стена справа от 4,4
  [[0, 0], "down", "inner"]    // Из 0,0 нельзя пойти вниз, но снизу прийти можно
]
```

### 🔴 Poison (Яд)
Если игрок коснется этого барьера — **МГНОВЕННАЯ СМЕРТЬ** (уровень перезапускается).
Идеально для создания узких коридоров, где нельзя ошибаться.
```json
"poison": [
  [[2, 2], "up", "both"],      // Смертельная линия над 2,2
  [[5, 5], "left", "outer"]    // Убьет, если попытаться войти в 5,5 слева
]
```

---

## 5. Примеры крутых механик

### "Точный тайминг"
Нужно попасть в центр ровно на 10-м шаге, а закончить в углу.
```json
"conditions": [
  { "check": "cell_has_steps", "cells": "center", "required_steps": [10] },
  { "check": "end_at", "cells": "corners" }
]
```

### "Пол — это лава"
Нельзя наступать в центр (негативное условие можно симулировать через Poison вокруг центра, либо через механику шагов).
*Вариант через Poison:* Окружить зону красными барьерами типа `outer`.

### "Бэктрекинг" (Возврат)
Нужно посетить клетку `[5,5]` на 3-ем шаге, а затем вернуться в неё же на 8-ом шаге.
```json
{
  "check": "cell_has_steps",
  "cells": [[5, 5]],
  "required_steps": [3, 8],
  "match": "all"
}
```

---

## 6. Тип Sequence (Устаревший)
Используется только для жестко заданных последовательностей нажатий.
```json
{
  "type": "sequence",
  "ans": "u u d d l r l r", // up, down, left, right
  ...
}
```
*Рекомендуется использовать `condition` вместо этого типа, так как sequence не вариативен.*

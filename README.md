# 📖 Руководство по созданию уровней

## Оглавление
1. [Структура файла](#структура-файла)
2. [Базовые параметры уровня](#базовые-параметры-уровня)
3. [Типы уровней](#типы-уровней)
4. [Система барьеров](#система-барьеров)
5. [Система условий](#система-условий)
6. [Логические группы](#логические-группы)
7. [Специальные значения cells](#специальные-значения-cells)
8. [Примеры уровней](#примеры-уровней)

---

## Структура файла

Все уровни хранятся в файле `levels.json` в корне проекта. Файл представляет собой JSON-массив объектов:

```json
[
  { "уровень 1" },
  { "уровень 2" },
  { "уровень 3" }
]
```

---

## Базовые параметры уровня

Каждый уровень — это объект со следующими полями:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `name` | string | ❌ | Название уровня (отображается в заголовке окна) |
| `type` | string | ❌ | Тип уровня: `"sequence"` или `"condition"`. По умолчанию `"sequence"` |
| `grid` | [int, int] | ❌ | Размер сетки [ширина, высота]. По умолчанию `[16, 12]` |
| `start` | [int, int] | ✅ | Стартовая позиция игрока [x, y] |
| `hint` | string | ❌ | Подсказка (показывается если игрок включил подсказки) |
| `ans` | string | ⚠️ | Последовательность ходов (только для `type: "sequence"`) |
| `conditions` | array | ⚠️ | Массив условий победы (только для `type: "condition"`) |
| `walls` | array | ❌ | Зелёные барьеры (блокируют проход) |
| `poison` | array | ❌ | Красные барьеры (убивают при касании) |

### Минимальный уровень

```json
{
  "start": [0, 0],
  "ans": "r r d d"
}
```

---

## Типы уровней

### 1. Sequence (последовательность)

Игрок должен выполнить **точную последовательность ходов**.

```json
{
  "name": "Простой путь",
  "type": "sequence",
  "grid": [8, 8],
  "start": [0, 0],
  "ans": "r r r d d d l l l u u u"
}
```

**Формат `ans`:** направления через пробел

| Значение | Альтернатива | Направление |
|----------|--------------|-------------|
| `u` | `up` | Вверх |
| `d` | `down` | Вниз |
| `l` | `left` | Влево |
| `r` | `right` | Вправо |

### 2. Condition (условия)

Игрок должен **выполнить все условия** из массива `conditions`.

```json
{
  "name": "Достигни цели",
  "type": "condition",
  "grid": [8, 8],
  "start": [0, 0],
  "conditions": [
    { "check": "end_at", "cells": [[7, 7]] }
  ]
}
```

---

## Система барьеров

Барьеры размещаются на **гранях клеток** и влияют на движение игрока.

### Структура барьера

```json
[[x, y], "сторона", "тип"]
```

| Параметр | Значения | Описание |
|----------|----------|----------|
| `[x, y]` | координаты | Клетка, которой принадлежит барьер |
| `сторона` | `"up"`, `"down"`, `"left"`, `"right"` | Грань клетки |
| `тип` | `"inner"`, `"outer"`, `"both"` | Направление блокировки |

### Типы барьеров

| Тип | Описание | Визуал |
|-----|----------|--------|
| `inner` | Блокирует **выход** из клетки | Линия + треугольник внутрь |
| `outer` | Блокирует **вход** в клетку | Линия + треугольник наружу |
| `both` | Блокирует в **обе стороны** | Толстая линия |

### Walls (стены) — зелёные

Блокируют проход. Игрок остаётся на месте, но шаг **засчитывается**.

```json
{
  "walls": [
    [[3, 2], "right", "both"],
    [[5, 5], "down", "inner"]
  ]
}
```

### Poison (яд) — красные

При касании игрок **умирает** и уровень перезапускается.

```json
{
  "poison": [
    [[4, 4], "up", "outer"],
    [[6, 3], "left", "both"]
  ]
}
```

### Визуальная схема барьеров

```
         outer (блокирует вход сверху)
              ▼
        ┌─────────┐
        │         │
 inner ◄│  [3,2]  │► outer
(выход) │         │  (вход)
        └─────────┘
              ▲
         inner (блокирует выход вниз)
```

---

## Система условий

Все условия проверяются одновременно. Уровень пройден когда **ВСЕ условия истинны**.

### Таблица всех типов условий

| check | Описание | Обязательные параметры | Опциональные |
|-------|----------|------------------------|--------------|
| `end_at` | Находиться в клетке в конце | `cells` | — |
| `visit_cells` | Посетить клетки | `cells` | `match` |
| `avoid_cells` | НЕ посещать клетки | `cells` | — |
| `total_steps` | Общее количество шагов | `count` | `operator` |
| `cell_has_steps` | Быть в клетке на конкретных шагах | `cells`, `required_steps` | `match` |
| `visit_count` | Посетить клетку N раз | `cells`, `count` | `operator`, `match` |
| `visit_order` | Посетить клетки в порядке | `cells` | — |
| `reach_before_step` | Достичь клетки ДО шага N | `cells`, `step` | `match` |
| `reach_after_step` | Достичь клетки НЕ РАНЬШЕ шага N | `cells`, `step` | `match` |
| `first_visit_at_step` | Первое посещение ровно на шаге N | `cells`, `step` | `match` |
| `last_visit_at_step` | Последнее посещение на шаге N | `cells`, `step` | `match` |
| `consecutive_visits` | Посетить N раз подряд | `cells`, `count` | `match` |
| `no_revisit` | Не посещать клетки дважды | — | `except` |
| `path_length_to_cell` | Шагов до первого достижения | `cells`, `count` | `operator`, `match` |
| `group` | Группа с логикой | `logic`, `items` | — |

### Параметр `match`

Определяет как проверять несколько клеток:

| Значение | Описание |
|----------|----------|
| `"any"` | Хотя бы одна клетка (по умолчанию) |
| `"all"` | Все клетки |

### Параметр `operator`

Оператор сравнения для числовых проверок:

| Значение | Описание |
|----------|----------|
| `"=="` | Равно (по умолчанию) |
| `"!="` | Не равно |
| `">"` | Больше |
| `"<"` | Меньше |
| `">="` | Больше или равно |
| `"<="` | Меньше или равно |

---

## Подробное описание условий

### `end_at` — финишная позиция

Игрок должен **находиться** в указанной клетке когда условия проверяются.

```json
{
  "check": "end_at",
  "cells": [[7, 7]]
}
```

---

### `visit_cells` — посещение клеток

Игрок должен **побывать** в указанных клетках (в любой момент).

```json
{
  "check": "visit_cells",
  "cells": [[3, 3], [5, 5]],
  "match": "all"
}
```

---

### `avoid_cells` — запретные клетки

Игрок **НЕ должен** заходить в указанные клетки.

```json
{
  "check": "avoid_cells",
  "cells": [[4, 4], [5, 5], [6, 6]]
}
```

---

### `total_steps` — количество шагов

Проверяет общее количество сделанных шагов.

```json
{
  "check": "total_steps",
  "count": 20,
  "operator": "<="
}
```

> ⚠️ **Важно:** шаг в стену (с `walls`) тоже считается!

---

### `cell_has_steps` — конкретные номера шагов

Игрок должен находиться в клетке **на конкретных шагах**.

```json
{
  "check": "cell_has_steps",
  "cells": [[3, 2]],
  "required_steps": [3, 8, 15]
}
```

> Шаг 0 — стартовая позиция.

---

### `visit_count` — количество посещений

Клетка должна быть посещена определённое количество раз.

```json
{
  "check": "visit_count",
  "cells": [[5, 5]],
  "count": 3,
  "operator": ">="
}
```

---

### `visit_order` — порядок посещения

Клетки должны быть посещены **в указанном порядке** (первое посещение каждой).

```json
{
  "check": "visit_order",
  "cells": [[0, 0], [5, 0], [5, 5], [0, 5]]
}
```

---

### `reach_before_step` — достичь до шага

Первое посещение клетки должно произойти **раньше** указанного шага.

```json
{
  "check": "reach_before_step",
  "cells": [[7, 7]],
  "step": 15
}
```

---

### `reach_after_step` — достичь не раньше шага

Первое посещение клетки должно произойти **на шаге N или позже**.

```json
{
  "check": "reach_after_step",
  "cells": [[3, 3]],
  "step": 10
}
```

---

### `first_visit_at_step` — первый визит ровно на шаге

Первое посещение должно произойти **ровно** на указанном шаге.

```json
{
  "check": "first_visit_at_step",
  "cells": [[5, 5]],
  "step": 12
}
```

---

### `consecutive_visits` — посещения подряд

Игрок должен **остаться** в клетке N ходов подряд (ходить в стену/границу).

```json
{
  "check": "consecutive_visits",
  "cells": [[0, 0]],
  "count": 3
}
```

---

### `no_revisit` — без повторных посещений

Каждая клетка посещается **максимум один раз** (кроме исключений).

```json
{
  "check": "no_revisit",
  "except": [[0, 0], [7, 7]]
}
```

---

### `path_length_to_cell` — длина пути до клетки

Количество шагов до **первого** достижения клетки.

```json
{
  "check": "path_length_to_cell",
  "cells": [[7, 7]],
  "count": 14,
  "operator": "=="
}
```

---

## Логические группы

Для сложной логики используйте `check: "group"`.

### Структура группы

```json
{
  "check": "group",
  "logic": "ОПЕРАТОР",
  "items": [
    { "условие 1" },
    { "условие 2" }
  ]
}
```

### Доступные операторы

| logic | Описание | Результат |
|-------|----------|-----------|
| `AND` | Логическое И | Все условия истинны |
| `OR` | Логическое ИЛИ | Хотя бы одно истинно |
| `NOT` | Отрицание | Первое условие ложно |
| `XOR` | Исключающее ИЛИ | Ровно одно условие истинно |
| `NAND` | НЕ-И | Не все условия истинны |
| `NOR` | НЕ-ИЛИ | Все условия ложны |

### Пример: OR

Посетить **либо** левый **либо** правый угол:

```json
{
  "check": "group",
  "logic": "OR",
  "items": [
    { "check": "visit_cells", "cells": [[0, 0]] },
    { "check": "visit_cells", "cells": [[7, 7]] }
  ]
}
```

### Пример: NOT

**Не** заканчивать в центре:

```json
{
  "check": "group",
  "logic": "NOT",
  "items": [
    { "check": "end_at", "cells": "center" }
  ]
}
```

### Пример: вложенные группы

```json
{
  "check": "group",
  "logic": "AND",
  "items": [
    {
      "check": "group",
      "logic": "OR",
      "items": [
        { "check": "visit_cells", "cells": [[0, 0]] },
        { "check": "visit_cells", "cells": [[7, 0]] }
      ]
    },
    {
      "check": "total_steps",
      "count": 20,
      "operator": "<="
    }
  ]
}
```

---

## Специальные значения cells

Вместо списка координат можно использовать ключевые слова:

| Значение | Описание | Эквивалент (для сетки 8×8) |
|----------|----------|---------------------------|
| `"corners"` | 4 угла | `[[0,0], [7,0], [0,7], [7,7]]` |
| `"edges"` | Все клетки по периметру | 28 клеток |
| `"center"` | Центральная клетка | `[[4, 4]]` |

### Пример использования

```json
{
  "check": "visit_cells",
  "cells": "corners",
  "match": "all"
}
```

---

## Примеры уровней

### 1. Простой sequence

```json
{
  "name": "Первые шаги",
  "type": "sequence",
  "grid": [5, 5],
  "start": [0, 0],
  "hint": "Просто иди вправо и вниз",
  "ans": "r r r r d d d d"
}
```

### 2. Лабиринт со стенами

```json
{
  "name": "Лабиринт",
  "type": "condition",
  "grid": [6, 6],
  "start": [0, 0],
  "hint": "Найди путь к выходу",
  "walls": [
    [[1, 0], "down", "both"],
    [[1, 1], "down", "both"],
    [[2, 2], "right", "both"],
    [[3, 1], "down", "both"]
  ],
  "conditions": [
    { "check": "end_at", "cells": [[5, 5]] }
  ]
}
```

### 3. Ядовитый коридор

```json
{
  "name": "Опасный путь",
  "type": "condition",
  "grid": [8, 4],
  "start": [0, 1],
  "poison": [
    [[2, 1], "up", "both"],
    [[2, 1], "down", "both"],
    [[5, 2], "up", "both"],
    [[5, 2], "down", "both"]
  ],
  "conditions": [
    { "check": "end_at", "cells": [[7, 2]] }
  ]
}
```

### 4. Сложный уровень с несколькими условиями

```json
{
  "name": "Мастер",
  "type": "condition",
  "grid": [8, 8],
  "start": [4, 4],
  "hint": "Посети углы по порядку, избегай центр, уложись в 30 шагов",
  "conditions": [
    {
      "check": "visit_order",
      "cells": [[0, 0], [7, 0], [7, 7], [0, 7]]
    },
    {
      "check": "avoid_cells",
      "cells": [[3, 3], [3, 4], [4, 3], [4, 4]]
    },
    {
      "check": "end_at",
      "cells": [[4, 4]]
    },
    {
      "check": "total_steps",
      "count": 30,
      "operator": "<="
    }
  ]
}
```

### 5. Выбор пути (OR логика)

```json
{
  "name": "Развилка",
  "type": "condition",
  "grid": [10, 5],
  "start": [0, 2],
  "hint": "Выбери верхний ИЛИ нижний путь",
  "walls": [
    [[4, 2], "left", "both"],
    [[4, 2], "right", "both"]
  ],
  "conditions": [
    {
      "check": "group",
      "logic": "OR",
      "items": [
        {
          "check": "group",
          "logic": "AND",
          "items": [
            { "check": "visit_cells", "cells": [[5, 0]] },
            { "check": "avoid_cells", "cells": [[5, 4]] }
          ]
        },
        {
          "check": "group",
          "logic": "AND",
          "items": [
            { "check": "visit_cells", "cells": [[5, 4]] },
            { "check": "avoid_cells", "cells": [[5, 0]] }
          ]
        }
      ]
    },
    { "check": "end_at", "cells": [[9, 2]] }
  ]
}
```

### 6. Простой пример

Заспавниться в (2,2), попасть в (3,2) на шагах 3 и 8, достичь (4,5) менее чем за 13 шагов:

```json
{
  "name": "Точный расчёт",
  "type": "condition",
  "grid": [8, 8],
  "start": [2, 2],
  "hint": "Шаги 3 и 8 в (3,2), достичь (4,5) до шага 12",
  "conditions": [
    {
      "check": "cell_has_steps",
      "cells": [[3, 2]],
      "required_steps": [3, 8]
    },
    {
      "check": "reach_before_step",
      "cells": [[4, 5]],
      "step": 13
    }
  ]
}
```

---

## Отладка уровней

### Режим разработчика

1. В игре нажмите **F9 + F11** одновременно
2. В консоли появится меню команд

### Команды консоли

| Команда | Описание |
|---------|----------|
| `1` или `show` | Показать записанную последовательность ходов |
| `2` или `clear` | Очистить запись |
| `3` или `coords` | Вкл/выкл отображение координат на сетке |
| `4` или `cells` | Показать какие шаги были на каких клетках |
| `help` | Показать меню |

### Горячие клавиши в игре

| Клавиша | Действие |
|---------|----------|
| `R` | Перезапустить уровень |
| `↑ ↓ ← →` | Движение |

---

## Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| Уровень не загружается | Синтаксическая ошибка JSON | Проверьте запятые и скобки |
| Игрок не может двигаться | `start` вне сетки | Убедитесь что `start` в пределах `grid` |
| Условие не срабатывает | Неверный `operator` | Проверьте `<=` vs `<` |
| Барьер не работает | Неверная `сторона` | `"up"` = верхняя грань клетки |

---

## Шаблон пустого уровня

```json
{
  "name": "Новый уровень",
  "type": "condition",
  "grid": [10, 10],
  "start": [0, 0],
  "hint": "",
  "walls": [],
  "poison": [],
  "conditions": [
    { "check": "end_at", "cells": [[9, 9]] }
  ]
}
```

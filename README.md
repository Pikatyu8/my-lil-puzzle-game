
# 🧩 Grid Puzzle Game

Логическая головоломка на сетке с гибким редактором уровней. Управляйте игроком, двигайте блоки, избегайте ловушек и выполняйте сложные условия для победы.

---

## 📑 Оглавление

1. [🚀 Как начать](#-как-начать)
2. [🎮 Управление и Сохранения](#-управление-и-сохранения)
3. [📦 Двигаемые объекты (Movable)](#-двигаемые-объекты-movable)
4. [📊 Визуальные обозначения](#-визуальные-обозначения)
5. [🛠️ Создание уровней (Гайд)](#️-создание-уровней-гайд)
   - [Структура файла](#структура-файла)
   - [Барьеры (Стены и Яд)](#барьеры-стены-и-яд)
   - [Коробки и Группы](#коробки-и-группы-movable)
   - [Условия победы](#условия-победы-conditions)
6. [✏️ Режим Редактирования](#️-режим-редактирования)
7. [⚙️ Режим Разработчика](#️-режим-разработчика)

---

## 🚀 Как начать

Запустите `game.py` (или `main.py`). В консоли выберите режим:

| Режим | Описание |
|-------|----------|
| **1** | Играть в основные уровни (`levels.json`) |
| **2** | Играть в пользовательские уровни (`user_levels.json`) |
| **3** | **Режим редактора** (быстрая отладка `edit_user_level.json`) |

---

## 🎮 Управление и Сохранения

### Основное
| Клавиша | Действие |
|---------|----------|
| `Стрелки` | Перемещение |
| `R` | **Рестарт** (мягкий) — сбрасывает позицию, но **сохраняет историю** (можно нажать Z) |
| `Shift+R` | **Полный сброс** — сбрасывает всё, очищает историю |
| `X` | Скрыть/показать текст требований на экране (режим просмотра) |

### Система отмены (Time Travel)
Игра запоминает каждое движение (игрока и коробок).

| Клавиша | Действие |
|---------|----------|
| `Z` | **Отмена хода (Undo)**. Работает даже после смерти или нажатия `R` |
| `S` | **Сохранить** состояние в слот (Quick Save) |
| `L` | **Загрузить** состояние из слота (Quick Load) |

> **Совет:** Если вы случайно умерли или застряли, просто нажмите `Z`.

---

## 📦 Двигаемые объекты (Movable)

На уровнях могут встречаться оранжевые блоки.
- **Толкание:** Просто идите на блок, чтобы сдвинуть его.
- **Цепочки:** Можно толкать несколько блоков сразу, стоящих в ряд.
- **Связанные группы:** Некоторые блоки "склеены" друг с другом (обозначены соединительной линией). Толкните один — сдвинется вся конструкция.
- **Блокировка:** Некоторые блоки нельзя толкать с определенных сторон (обозначено красной чертой на блоке).

---

## 📊 Визуальные обозначения

| Символ | Цвет | Значение |
|--------|------|----------|
| **◎** | Зеленый | **Финиш**. Здесь нужно закончить уровень. |
| **•** | Желтый | **Обязательно посетить**. |
| **✕** | Красный | **Не посещать** (или посетить 0 раз). |
| **1, 2...** | Синий | Порядок посещения. |
| **✓2n** | Зеленый | Быть здесь на чётных шагах. |
| **⊘3-5** | Розовый | Не быть здесь на шагах с 3 по 5. |
| **⟳3** | Оранж | Оставаться в клетке 3 хода подряд. |
| **∞** | Желтый | Клетка-исключение (в неё можно заходить много раз при правиле "Без повторов"). |

**Глобальная панель (справа):**
- **Шагов: =N** — нужно сделать ровно N шагов.
- **Sequence** — нужно выполнить последовательность нажатий (например, `↑ ↓ ← →`).

---

## 🛠️ Создание уровней (Гайд)

Уровни хранятся в JSON файлах. Вот полная документация по созданию своих карт.

### Структура файла

```json
[
  {
    "name": "Название уровня",
    "grid": [16, 12],      // [Ширина, Высота]
    "start": [0, 0],       // [X, Y] старта
    "hint": "Подсказка",
    "walls": [ ... ],      // Стены
    "poison": [ ... ],     // Яд
    "movable": [ ... ],    // Коробки
    "conditions": [ ... ]  // Условия победы
  }
]
```

### Барьеры (Стены и Яд)

Массивы `"walls"` и `"poison"` работают одинаково.

**Параметры:**
- `cell` / `cells`: Координаты точек.
- `range` / `ranges`: Прямоугольные области `[[x1,y1], [x2,y2]]`.
- `sides`: Стороны барьера (`"u"`, `"d"`, `"l"`, `"r"`, `"all"`).
- `type`: `"both"` (стена), `"inner"` (нельзя выйти), `"outer"` (нельзя войти).
- `mode`: `"fill"` (залить область) или `"perimeter"` (только по контуру).

**Примеры:**

```json
"walls": [
  // Одна клетка, стена сверху
  { "cell": [5, 5], "sides": "u" },

  // Прямоугольник 3x3, полностью заполненный стенами
  { "range": [[0, 0], [2, 2]], "sides": "all" },

  // Периметр (коробка), стены только по границам прямоугольника
  { "range": [[5, 5], [10, 10]], "sides": "all", "mode": "perimeter" }
]
```

### Коробки и Группы (Movable)

Добавьте массив `"movable"`.

**Параметры:**
- `cell` / `cells` / `range`: Где стоит коробка.
- `blocked`: Стороны, с которых **нельзя** толкать (`"u"`, `"d"`, `"l"`, `"r"`).
- `can_push`: `true`/`false` (может ли эта коробка толкать другие).
- `can_be_pushed_by`: `true`/`false` (могут ли другие коробки толкать эту).
- `connected`: `true`/`false` (**Новое!**) — объединяет все объекты из этого определения в одну группу.

**Примеры:**

```json
"movable": [
  // Простая коробка
  { "cell": [4, 4] },

  // Длинный "диван" (2x1), который двигается как единое целое
  { "range": [[5, 5], [6, 5]], "connected": true },

  // Группа из 3-х отдельных блоков, которые двигаются синхронно
  { "cells": [[2, 2], [4, 2], [2, 4]], "connected": true },

  // Тяжелый ящик, который нельзя толкать снизу (нужно обойти)
  { "cell": [8, 8], "blocked": "down" }
]
```

### Условия победы (Conditions)

Самая мощная часть редактора. Массив `"conditions"`.

#### 1. Основные проверки
| Тип (`check`) | Параметры | Описание |
|---|---|---|
| `end_at` | `cells` | Закончить уровень в указанных клетках. |
| `total_steps` | `count`, `operator` | Ограничение ходов (напр. `< 20`). |
| `no_revisit` | `except` | Запрет наступать на одну клетку дважды (кроме `except`). |
| `sequence` | `moves`, `mode` | Выполнить серию нажатий (напр. `"u u d d"`). |

#### 2. Посещение (`visit`)
Контроль количества заходов в клетки.
- `count`: Сколько раз посетить (0 = избегать).
- `operator`: `==`, `>`, `<`, `>=`, `<=`.

```json
// Избегать (Яд, но без смерти, просто условие)
{ "check": "visit", "cells": [[1,1]], "count": 0 }

// Посетить минимум 1 раз (собрать монетку)
{ "check": "visit", "cells": [[2,2]], "count": 1, "operator": ">=" }
```

#### 3. Шаги и время (`at_steps`)
Где игрок должен (или не должен) быть в конкретный момент времени.
- `mode`: `"require"` (должен быть) или `"avoid"` (не должен).
- `step`: Конкретный номер хода.
- `step_expr`: Формула (`even` (чет), `odd` (нечет), `prime` (простые), `div:5`, `range:10:20`).

```json
// Быть в центре на 10-м ходу
{ "check": "at_steps", "cells": [[5,5]], "step": 10 }

// Не наступать на опасные клетки на чётных ходах
{ "check": "at_steps", "cells": [[1,1]], "step_expr": "even", "mode": "avoid" }
```

#### 4. Порядок и логика
- `order`: Посетить клетки в строгом порядке `[A, B, C]`.
- `consecutive`: Находиться в клетке N ходов подряд (топтаться).
- `group`: Объединить условия через `AND`, `OR`, `NOT`, `XOR`.

---

## ✏️ Режим Редактирования

Идеален для создания уровней без перезапуска игры.

1. Запустите игру в **режиме 3**.
2. Откройте файл `edit_user_level.json` в любом текстовом редакторе.
3. Измените уровень (добавьте стену, условие или коробку).
4. Сохраните файл.
5. В игре нажмите **Enter**.
6. Уровень мгновенно перезагрузится с новыми параметрами!

> Если файла `edit_user_level.json` нет, игра создаст его сама с примерами всех механик.

---

## ⚙️ Режим Разработчика

Нажмите `F9` + `F11` во время игры. В консоль можно вводить команды:

- `1` — Показать записанный путь (полезно для создания sequence уровней).
- `2` — Очистить путь.
- `3` — Включить **координаты** на клетках (оранжевые цифры).
- `4` — Показать номера ходов на каждой клетке.
- `5` — Отключить победу (чтобы тестить условия и не выигрывать случайно).

---

### Примеры координат `cells`
Везде, где требуются координаты, можно писать:
- `[[x, y]]` — список точек.
- `"corners"` — 4 угла.
- `"edges"` — весь периметр.
- `"center"` — центр карты.

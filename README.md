# Руководство по Grid Puzzle Game

## 📑 Оглавление

- [🚀 Запуск и загрузка уровней](#-запуск-и-загрузка-уровней)
- [🎮 О игре](#-о-игре)
- [⌨️ Управление](#️-управление)
- [📊 Визуальные обозначения](#-визуальные-обозначения)
- [🎯 Типы уровней](#-типы-уровней)
- [📝 Создание уровней (Редактор карт)](#-создание-уровней-редактор-карт)
  - [Базовая структура JSON](#базовая-структура-json)
  - [Система барьеров](#система-барьеров-walls--poison)
  - [Система условий](#система-условий-conditions)
  - [Параметры шагов](#параметры-шагов)
  - [Специальные значения cells](#специальные-значения-cells)
  - [Группировка условий](#группировка-условий-check-group)
- [🛠️ Режим разработчика](#️-режим-разработчика)
- [📋 Примеры уровней](#-примеры-уровней)

---

## 🚀 Запуск и загрузка уровней

При запуске игры в консоли предлагается:

1. **Загрузить свои уровни:** Введите `y`, чтобы загрузить `user_levels.json` из папки с игрой.
2. **Включить подсказки:** Введите `y`, чтобы видеть текст `"hint"` в консоли.
3. **Выбрать уровень:** Введите номер или нажмите Enter для первого уровня.

---

## 🎮 О игре

Grid Puzzle Game — логическая головоломка на сетке. Управляйте белым кругом, выполняйте условия, избегайте ловушек.

---

## ⌨️ Управление

| Клавиша | Действие |
|---------|----------|
| `↑ ↓ ← →` | Движение |
| `R` | Перезапуск уровня |
| `X` | Показать/скрыть требования |
| `F9 + F11` | Активация режима разработчика |

---

## 📊 Визуальные обозначения

### Иконки на клетках

| Символ | Цвет | Значение |
|--------|------|----------|
| **✕** | Красный | Запрет посещения |
| **◎** | Зелёный | Финишная клетка |
| **•** | Жёлтый | Обязательное посещение |
| **1, 2, 3...** | Голубой | Порядок посещения |
| **×≥N / ×≤N** | Жёлтый | Минимум/максимум посещений |
| **⟳N** | Жёлтый | Подряд N посещений |
| **∞** | Жёлтый | Исключение из запрета повторов |
| **⊘...** | Розовый | Запрет на указанных шагах |
| **✓...** | Зелёный | Требование на указанных шагах |

### Барьеры

| Цвет | Тип |
|------|-----|
| 🟩 Зелёный | Стена (блокирует проход) |
| 🔴 Красный | Яд (мгновенная смерть) |

### Глобальные требования (правый верхний угол)

| Текст | Значение |
|-------|----------|
| **Шагов: =N** | Лимит ходов |
| **Без повторов** | Каждая клетка только 1 раз |

---

## 🎯 Типы уровней

### Sequence (Последовательность)
```json
{
  "type": "sequence",
  "start": [0, 0],
  "ans": "r r d d l l"
}
```
Игрок должен угадать/найти точную последовательность ходов. Серая клетка показывает конечную точку.

### Condition (Условия)
```json
{
  "type": "condition",
  "start": [0, 0],
  "conditions": [...]
}
```
Игрок видит требования и должен спланировать маршрут.

---

## 📝 Создание уровней (Редактор карт)

### Базовая структура JSON

```json
{
  "name": "Название уровня",
  "type": "condition",
  "grid": [16, 12],
  "start": [0, 0],
  "hint": "Текст подсказки",
  "walls": [],
  "poison": [],
  "wall_is_poison": false,
  "conditions": []
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `name` | string | Название уровня |
| `type` | string | `"sequence"` или `"condition"` |
| `grid` | [int, int] | Размер сетки [ширина, высота], по умолчанию [16, 12] |
| `start` | [int, int] | Начальная позиция [x, y] |
| `hint` | string | Подсказка (показывается если включены) |
| `ans` | string | Ответ для sequence (например `"r r d l"`) |
| `walls` | array | Стены |
| `poison` | array | Ядовитые барьеры |
| `wall_is_poison` | bool/object | Если `true`, все стены становятся ядом. Можно указать `{"except": [[x,y]]}` |
| `conditions` | array | Условия для condition-уровней |

---

### Система барьеров (walls / poison)

Формат барьера:
```json
[ координаты, { "стороны": "тип" } ]
```

#### Координаты

```json
// Одна клетка
[[5, 3], {"up": "both"}]

// Несколько клеток
[[[1,1], [2,1], [3,1]], {"down": "both"}]
```

#### Стороны

| Значение | Описание |
|----------|----------|
| `"u"` или `"up"` | Верх |
| `"d"` или `"down"` | Низ |
| `"l"` или `"left"` | Лево |
| `"r"` или `"right"` | Право |
| `"square"`, `"all"`, `"box"` | Все 4 стороны |
| `"ud"`, `"lr"`, `"uldr"` | Комбинации букв |

#### Типы барьеров

| Тип | Описание | Визуализация |
|-----|----------|--------------|
| `"inner"` | Нельзя **выйти** из клетки | Стрелка внутрь |
| `"outer"` | Нельзя **войти** в клетку | Стрелка наружу |
| `"both"` | Полная блокировка | Толстая линия |

#### Примеры барьеров

```json
"walls": [
  // Полная стена сверху клетки [2,3]
  [[2, 3], {"up": "both"}],
  
  // Можно войти справа, но нельзя выйти
  [[5, 5], {"right": "inner"}],
  
  // Нельзя войти сверху
  [[7, 2], {"up": "outer"}],
  
  // Стены со всех сторон
  [[4, 4], {"square": "both"}],
  
  // Стены сверху и снизу
  [[3, 3], {"ud": "both"}],
  
  // Несколько клеток с одинаковой стеной
  [[[0,0], [1,0], [2,0]], {"down": "both"}]
]
```

---

### Система условий (conditions)

Все условия добавляются в массив `"conditions"`. Для победы должны выполниться **все** условия.

---

#### `check: "visit"` — Посещение клеток

Проверяет количество посещений указанных клеток.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `cells` | array | Список клеток или спецзначение |
| `count` | int | Требуемое количество посещений (по умолчанию 1) |
| `operator` | string | Оператор сравнения: `"=="`, `">="`, `"<="`, `">"`, `"<"`, `"!="` |
| `min` / `max` | int | Альтернатива: диапазон посещений |
| `match` | string | `"all"` (все клетки) или `"any"` (любая) |

**Примеры:**

```json
// Посетить клетку минимум 1 раз (иконка •)
{"check": "visit", "cells": [[2, 2]]}

// Никогда не заходить (иконка ✕)
{"check": "visit", "cells": [[3, 3]], "count": 0, "operator": "=="}

// Посетить ровно 3 раза
{"check": "visit", "cells": [[1, 1]], "count": 3, "operator": "=="}

// Посетить от 2 до 5 раз
{"check": "visit", "cells": [[4, 4]], "min": 2, "max": 5}

// Посетить любую из клеток
{"check": "visit", "cells": [[0,0], [5,5]], "match": "any"}
```

---

#### `check: "at_steps"` — Требования на конкретных шагах

Проверяет нахождение в клетках на определённых шагах.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `cells` | array | Список клеток |
| `mode` | string | `"require"` (обязательно быть) или `"avoid"` (запрещено быть) |
| `step` | int | Конкретный шаг |
| `steps` | array | Список шагов |
| `step_range` | [int, int] | Диапазон [от, до] включительно |
| `step_expr` | string | Выражение для шагов (см. ниже) |
| `match` | string | `"all"` или `"any"` |

**Примеры:**

```json
// Быть в клетке на шаге 5 (иконка ✓5)
{"check": "at_steps", "cells": [[2, 2]], "step": 5, "mode": "require"}

// Запрещено быть на шагах 3-7 (иконка ⊘3-7)
{"check": "at_steps", "cells": [[1, 1]], "step_range": [3, 7], "mode": "avoid"}

// Быть на шагах 0, 5, 10
{"check": "at_steps", "cells": [[3, 3]], "steps": [0, 5, 10], "mode": "require"}

// Быть на каждом чётном шаге
{"check": "at_steps", "cells": [[4, 4]], "step_expr": "even", "mode": "require"}
```

---

#### `check: "end_at"` — Финишная клетка

Уровень завершается только если игрок находится в одной из указанных клеток.

```json
{"check": "end_at", "cells": [[7, 5]]}

// Закончить в любом углу
{"check": "end_at", "cells": "corners"}
```

---

#### `check: "order"` — Порядок посещения

Клетки должны быть посещены строго в указанном порядке (по первому входу).

```json
// Сначала [0,0], потом [5,0], потом [5,5]
{"check": "order", "cells": [[0, 0], [5, 0], [5, 5]]}
```

---

#### `check: "consecutive"` — Подряд посещения

Игрок должен "топтаться" на клетке N ходов подряд.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `cells` | array | Клетки |
| `count` | int | Минимум последовательных посещений (по умолчанию 2) |
| `match` | string | `"all"` или `"any"` |

```json
// Остаться в клетке минимум 3 хода подряд (иконка ⟳3)
{"check": "consecutive", "cells": [[2, 2]], "count": 3}
```

---

#### `check: "no_revisit"` — Запрет повторов

Запрещает посещать любую клетку дважды.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `except` | array | Клетки-исключения (можно посещать повторно) |

```json
// Без повторов
{"check": "no_revisit"}

// Без повторов, кроме [0,0] и [5,5]
{"check": "no_revisit", "except": [[0, 0], [5, 5]]}
```

---

#### `check: "total_steps"` — Лимит шагов

Ограничивает общее количество ходов.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `count` | int | Количество шагов |
| `operator` | string | `"=="`, `">="`, `"<="`, `">"`, `"<"` |

```json
// Ровно 15 шагов
{"check": "total_steps", "count": 15, "operator": "=="}

// Не более 20 шагов
{"check": "total_steps", "count": 20, "operator": "<="}
```

---

### Параметры шагов

Для условия `at_steps` доступны разные способы указания шагов:

| Параметр | Пример | Описание |
|----------|--------|----------|
| `step` | `5` | Один конкретный шаг |
| `steps` | `[1, 3, 5, 7]` | Список шагов |
| `step_range` | `[2, 8]` | Диапазон от 2 до 8 включительно |
| `step_expr` | `"even"` | Выражение (см. ниже) |

#### Выражения step_expr

| Выражение | Описание |
|-----------|----------|
| `even` | Чётные шаги (0, 2, 4, 6...) |
| `odd` | Нечётные шаги (1, 3, 5, 7...) |
| `prime` | Простые числа (2, 3, 5, 7, 11...) |
| `div:N` | Делится на N (div:3 = 0, 3, 6, 9...) |
| `mod:N:R` | Остаток от деления на N равен R |
| `range:A:B` | Диапазон от A до B |
| `gt:N` | Больше N |
| `lt:N` | Меньше N |
| `gte:N` | Больше или равно N |
| `lte:N` | Меньше или равно N |

**Логические операторы в выражениях:**

| Оператор | Описание |
|----------|----------|
| `\|` | ИЛИ |
| `&` | И |
| `!` | НЕ |

**Примеры выражений:**

```json
// Чётные шаги
"step_expr": "even"

// Чётные ИЛИ делящиеся на 3
"step_expr": "even|div:3"

// Чётные И больше 5
"step_expr": "even&gt:5"

// НЕ чётные (аналог odd)
"step_expr": "!even"

// Простые числа больше 10
"step_expr": "prime&gt:10"
```

---

### Специальные значения cells

Вместо списка координат можно использовать:

| Значение | Описание |
|----------|----------|
| `"corners"` | Четыре угла поля |
| `"edges"` | Весь периметр |
| `"center"` | Центр поля |

```json
{"check": "end_at", "cells": "corners"}
{"check": "visit", "cells": "edges", "count": 0, "operator": "=="}
```

---

### Группировка условий (check: "group")

Объединяет несколько условий логическими операторами.

| Параметр | Тип | Описание |
|----------|-----|----------|
| `logic` | string | `"AND"`, `"OR"`, `"NOT"`, `"XOR"` |
| `items` | array | Вложенные условия |

```json
{
  "check": "group",
  "logic": "OR",
  "items": [
    {"check": "end_at", "cells": [[0, 5]]},
    {"check": "end_at", "cells": [[7, 5]]}
  ]
}
```

| Логика | Описание |
|--------|----------|
| `AND` | Все условия должны выполниться |
| `OR` | Хотя бы одно условие |
| `NOT` | Инверсия первого условия |
| `XOR` | Ровно одно условие |

---

## 🛠️ Режим разработчика

**Активация:** `F9 + F11` во время игры.

**Команды консоли:**

| Команда | Описание |
|---------|----------|
| `1` | Показать записанную последовательность ходов |
| `2` | Очистить запись ходов |
| `3` | Вкл/выкл отображение координат |
| `4` | Статистика посещения клеток |
| `5` | Отключить победу (для тестирования) |
| `help` | Список команд |

---

## 📋 Примеры уровней

### Простой sequence-уровень

```json
{
  "name": "Первые шаги",
  "type": "sequence",
  "grid": [8, 6],
  "start": [0, 0],
  "ans": "r r r d d d",
  "hint": "Двигайтесь к правому нижнему углу"
}
```

### Condition: посещение и финиш

```json
{
  "name": "Собери и финишируй",
  "type": "condition",
  "grid": [10, 8],
  "start": [0, 0],
  "conditions": [
    {"check": "visit", "cells": [[5, 4], [2, 6]]},
    {"check": "end_at", "cells": [[9, 7]]}
  ]
}
```

### Condition: порядок и лимит шагов

```json
{
  "name": "По порядку",
  "type": "condition",
  "grid": [8, 8],
  "start": [0, 0],
  "conditions": [
    {"check": "order", "cells": [[2, 2], [5, 2], [5, 5], [2, 5]]},
    {"check": "total_steps", "count": 20, "operator": "<="}
  ]
}
```

### Condition: избегай на чётных шагах

```json
{
  "name": "Чётная ловушка",
  "type": "condition",
  "grid": [6, 6],
  "start": [0, 0],
  "conditions": [
    {"check": "at_steps", "cells": [[3, 3]], "step_expr": "even", "mode": "avoid"},
    {"check": "visit", "cells": [[3, 3]]},
    {"check": "end_at", "cells": [[5, 5]]}
  ],
  "hint": "Посети центр на нечётном шаге"
}
```

### Лабиринт со стенами и ядом

```json
{
  "name": "Опасный лабиринт",
  "type": "condition",
  "grid": [8, 8],
  "start": [0, 0],
  "walls": [
    [[2, 0], {"down": "both"}],
    [[[4, 2], [4, 3], [4, 4]], {"right": "both"}]
  ],
  "poison": [
    [[3, 3], {"square": "both"}]
  ],
  "conditions": [
    {"check": "end_at", "cells": [[7, 7]]},
    {"check": "no_revisit"}
  ]
}
```

---

> 💡 **Совет:** Используйте режим разработчика (команда `4`) чтобы увидеть, какие шаги были сделаны в каждой клетке — это помогает при создании условий `at_steps`.

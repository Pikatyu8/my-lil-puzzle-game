# 🧩 Grid Puzzle Game

Логическая головоломка на сетке с гибким редактором уровней. Управляйте игроком, двигайте блоки, избегайте ловушек и выполняйте сложные условия для победы.

---

## 📑 Оглавление

1. [🚀 Как начать](#-как-начать)
2. [🎮 Управление и Сохранения](#-управление-и-сохранения)
3. [📦 Двигаемые объекты (Movable)](#-двигаемые-объекты-movable)
4. [📊 Визуальные обозначения](#-визуальные-обозначения)
5. [🛠️ Создание уровней (Гайд)](#️-создание-уровней-гайд)
   - [Структура файла](#структура-файла)
   - [Барьеры (Стены и Яд)](#барьеры-стены-и-яд)
   - [Коробки и Группы](#коробки-и-группы-movable)
   - [Условия победы](#условия-победы-conditions)
6. [✏️ Режим Редактирования](#️-режим-редактирования)
7. [⚙️ Режим Разработчика](#️-режим-разработчика)
8. [❓ FAQ и Решение проблем](#-faq-и-решение-проблем)

---

## 🚀 Как начать

Запустите `main.py`. В консоли выберите режим:

| Режим | Описание |
|-------|----------|
| **1** | Играть в основные уровни (`levels.json`) |
| **2** | Играть в пользовательские уровни (`user_levels.json`) |
| **3** | **Режим редактора** (быстрая отладка `edit_user_level.json`) |

---

## 🎮 Управление и Сохранения

### Основное
| Клавиша | Действие |
|---------|----------|
| `Стрелки` | Перемещение |
| `R` | **Рестарт** (мягкий) — сбрасывает позицию, но **сохраняет историю** (можно нажать Z) |
| `Shift+R` | **Полный сброс** — сбрасывает всё, очищает историю |
| `X` | Скрыть/показать текст требований на экране (режим просмотра) |

### Система отмены (Time Travel)
Игра запоминает каждое движение (игрока и коробок).

| Клавиша | Действие |
|---------|----------|
| `Z` | **Отмена хода (Undo)**. Работает даже после смерти или нажатия `R` |
| `S` | **Сохранить** состояние в слот (Quick Save) |
| `L` | **Загрузить** состояние из слота (Quick Load) |

> **Совет:** Если вы случайно умерли или застряли, просто нажмите `Z`.

---

## 📦 Двигаемые объекты (Movable)

На уровнях могут встречаться оранжевые блоки.
- **Толкание:** Просто идите на блок, чтобы сдвинуть его.
- **Цепочки:** Можно толкать несколько блоков сразу, стоящих в ряд.
- **Связанные группы:** Некоторые блоки "склеены" друг с другом (обозначены соединительной линией). Толкните один — сдвинется вся конструкция.
- **Блокировка:** Некоторые блоки нельзя толкать с определенных сторон (обозначено красной чертой на блоке).

---

## 📊 Визуальные обозначения

### На игровом поле

| Символ | Цвет | Значение |
|--------|------|----------|
| **◎** | Зеленый | **Финиш**. Здесь нужно закончить уровень. |
| **•** | Желтый | **Обязательно посетить**. |
| **✕** | Красный | **Не посещать** (или посетить 0 раз). |
| **1, 2...** | Синий | Порядок посещения. |
| **×≥3** | Желтый | Посетить минимум 3 раза. |
| **✓2n** | Зеленый | Быть здесь на чётных шагах. |
| **⊘3-5** | Розовый | Не быть здесь на шагах с 3 по 5. |
| **⟳3** | Оранжевый | Оставаться в клетке 3 хода подряд. |
| **∞** | Желтый | Клетка-исключение (можно заходить много раз при правиле "Без повторов"). |

### Глобальная панель (справа)

| Текст | Значение |
|-------|----------|
| **Шагов: =N** | Нужно сделать ровно N шагов |
| **Шагов: ≤N** | Не больше N шагов |
| **Без повторов** | Нельзя наступать на одну клетку дважды |
| **∋↑↓←→** | Путь должен содержать эту последовательность |
| **∌↑↑** | Путь НЕ должен содержать эту последовательность |
| **≡↑↓↑** | Путь должен быть РОВНО таким |

---

## 🛠️ Создание уровней (Гайд)

Уровни хранятся в JSON файлах. Вот полная документация по созданию своих карт.

### Структура файла

```json
[
  {
    "name": "Название уровня",
    "grid": [16, 12],
    "start": [0, 0],
    "hint": "Подсказка",
    "walls": [ ... ],
    "poison": [ ... ],
    "movable": [ ... ],
    "conditions": [ ... ]
  }
]
```

| Поле | Обязательно | Описание |
|------|-------------|----------|
| `name` | Нет | Название уровня |
| `grid` | Нет | `[Ширина, Высота]`, по умолчанию `[16, 12]` |
| `start` | **Да** | `[X, Y]` стартовой позиции |
| `hint` | Нет | Текст подсказки |
| `walls` | Нет | Массив стен |
| `poison` | Нет | Массив ядовитых барьеров |
| `movable` | Нет | Массив двигаемых объектов |
| `conditions` | Нет | Массив условий победы |

---

### Барьеры (Стены и Яд)

Массивы `"walls"` и `"poison"` работают одинаково. Стены блокируют движение, яд убивает при касании.

#### Параметры барьеров

| Параметр | Описание | Примеры |
|----------|----------|---------|
| `cell` | Одна клетка | `[5, 5]` |
| `cells` | Список клеток | `[[1,1], [2,2], [3,3]]` |
| `range` | Прямоугольник | `[[0,0], [5,5]]` |
| `ranges` | Несколько прямоугольников | `[[[0,0], [2,2]], [[5,5], [7,7]]]` |
| `sides` | Стороны барьера | `"u"`, `"d"`, `"l"`, `"r"`, `"ud"`, `"all"` |
| `type` | Тип блокировки | `"both"`, `"inner"`, `"outer"` |
| `mode` | Режим заполнения | `"fill"` (по умолчанию), `"perimeter"` |
| `except` | Исключения | Вложенные определения клеток |

#### Типы блокировки (`type`)

| Тип | Описание |
|-----|----------|
| `both` | Полная стена — нельзя ни войти, ни выйти |
| `inner` | Нельзя **выйти** из клетки через эту сторону |
| `outer` | Нельзя **войти** в клетку через эту сторону |

#### Примеры барьеров

```json
"walls": [
  // Одна клетка, стена сверху
  { "cell": [5, 5], "sides": "u", "type": "both" },

  // Прямоугольник, заполненный стенами со всех сторон
  { "range": [[0, 0], [2, 2]], "sides": "all", "type": "both" },

  // Периметр (только границы прямоугольника)
  { "range": [[5, 5], [10, 10]], "mode": "perimeter", "sides": "all" },

  // Несколько отдельных клеток
  { "cells": [[1,1], [3,3], [5,5]], "sides": "lr", "type": "both" },

  // С исключениями
  { 
    "range": [[0, 0], [5, 5]], 
    "sides": "all",
    "except": [
      { "cell": [2, 2], "sides": "d" }
    ]
  }
]
```

```json
"poison": [
  // Яд по периметру области (ловушка)
  { "range": [[0, 5], [9, 10]], "mode": "perimeter", "sides": "all", "type": "both" },

  // Односторонний яд — убивает при попытке выйти
  { "cell": [3, 3], "sides": "u", "type": "inner" },

  // Яд на входе — убивает при попытке войти
  { "range": [[5, 5], [7, 7]], "sides": "d", "type": "outer" }
]
```

> **Важно:** Яд работает на границах уровня! Если у края карты стоит `inner` яд, игрок погибнет при попытке выйти за границу.

---

### Коробки и Группы (Movable)

#### Параметры объектов

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `cell` | Одна клетка | — |
| `cells` | Список клеток | — |
| `range` | Прямоугольник | — |
| `blocked` | Стороны, с которых нельзя толкать | `""` (можно отовсюду) |
| `can_push` | Может ли толкать другие коробки | `true` |
| `can_be_pushed_by` | Могут ли другие коробки толкать эту | `true` |
| `connected` | Объединить в группу | `false` |

#### Примеры

```json
"movable": [
  // Простая коробка
  { "cell": [4, 4] },

  // Длинный блок 3x1, двигается как единое целое
  { "range": [[5, 5], [7, 5]], "connected": true },

  // L-образная фигура из 3 блоков
  { "cells": [[2, 2], [2, 3], [3, 3]], "connected": true },

  // Тяжёлый ящик — нельзя толкать снизу
  { "cell": [8, 8], "blocked": "d" },

  // Неподвижный блок (препятствие)
  { "cell": [1, 1], "blocked": "udlr" },

  // Блок, который не может толкать другие
  { "cell": [3, 3], "can_push": false }
]
```

---

### Условия победы (Conditions)

Самая мощная часть редактора. Все условия должны быть выполнены для победы.

#### Базовые проверки

| Тип (`check`) | Описание |
|---------------|----------|
| `end_at` | Закончить в указанных клетках |
| `visit` | Посетить клетки определённое число раз |
| `total_steps` | Ограничение на общее количество ходов |
| `no_revisit` | Запрет повторного посещения клеток |
| `order` | Посетить клетки в строгом порядке |
| `at_steps` | Быть в клетках на определённых шагах |
| `consecutive` | Оставаться в клетке N ходов подряд |
| `sequence` | Выполнить последовательность нажатий |
| `group` | Логическое объединение условий |

---

#### `end_at` — Финиш

```json
{ "check": "end_at", "cells": [[9, 9]] }

// Финиш в любом углу
{ "check": "end_at", "cells": "corners" }
```

---

#### `visit` — Посещение клеток

| Параметр | Описание |
|----------|----------|
| `cells` | Список клеток или `range` |
| `count` | Количество посещений |
| `operator` | `==`, `>=`, `<=`, `>`, `<`, `!=` |
| `min` / `max` | Диапазон посещений |
| `match` | `"all"` (все клетки) или `"any"` (любая) |

```json
// Посетить клетку ровно 1 раз
{ "check": "visit", "cells": [[5, 5]], "count": 1, "operator": "==" }

// Избегать клетки (посетить 0 раз)
{ "check": "visit", "cells": [[1, 1], [2, 2]], "count": 0 }

// Посетить каждую клетку от 2 до 5 раз
{ "check": "visit", "cells": [[3, 3], [4, 4]], "min": 2, "max": 5 }

// Посетить прямоугольную область (все клетки)
{ "check": "visit", "range": [[0, 0], [3, 3]], "count": 1, "operator": ">=" }

// Посетить хотя бы одну из клеток
{ "check": "visit", "cells": [[1, 1], [9, 9]], "match": "any" }
```

---

#### `total_steps` — Ограничение ходов

```json
// Ровно 20 шагов
{ "check": "total_steps", "count": 20, "operator": "==" }

// Не больше 50 шагов
{ "check": "total_steps", "count": 50, "operator": "<=" }

// Минимум 10 шагов
{ "check": "total_steps", "count": 10, "operator": ">=" }
```

---

#### `no_revisit` — Без повторов

```json
// Полный запрет повторов
{ "check": "no_revisit" }

// С исключениями (в эти клетки можно заходить много раз)
{ "check": "no_revisit", "except": [[0, 0], [5, 5]] }
```

---

#### `order` — Порядок посещения

```json
// Посетить A, потом B, потом C (в любой момент, но в этом порядке)
{ "check": "order", "cells": [[0, 0], [5, 5], [9, 9]] }
```

---

#### `at_steps` — Позиция на шаге

| Параметр | Описание |
|----------|----------|
| `step` | Конкретный номер шага |
| `steps` | Список шагов |
| `step_range` | Диапазон `[от, до]` |
| `step_expr` | Формула (см. ниже) |
| `mode` | `"require"` (должен быть) или `"avoid"` (не должен) |

**Формулы `step_expr`:**

| Формула | Значение |
|---------|----------|
| `even` | Чётные шаги (0, 2, 4, ...) |
| `odd` | Нечётные шаги (1, 3, 5, ...) |
| `prime` | Простые числа (2, 3, 5, 7, 11, ...) |
| `div:N` | Кратные N (div:3 → 0, 3, 6, 9, ...) |
| `mod:N:M` | Остаток от деления на N равен M |
| `range:A:B` | Шаги от A до B включительно |
| `gt:N` / `lt:N` | Больше / меньше N |
| `gte:N` / `lte:N` | Больше-равно / меньше-равно N |

Формулы можно комбинировать: `even&gt:10` (чётные после 10), `odd|prime` (нечётные ИЛИ простые).

```json
// Быть в клетке на 10-м шаге
{ "check": "at_steps", "cells": [[5, 5]], "step": 10 }

// Быть в клетке на шагах 5, 10, 15
{ "check": "at_steps", "cells": [[3, 3]], "steps": [5, 10, 15] }

// Не наступать на клетку на чётных шагах
{ "check": "at_steps", "cells": [[1, 1]], "step_expr": "even", "mode": "avoid" }

// Быть на клетке каждый 5-й шаг
{ "check": "at_steps", "cells": [[0, 0]], "step_expr": "div:5" }
```

---

#### `consecutive` — Топтание на месте

```json
// Оставаться в клетке 3 хода подряд
{ "check": "consecutive", "cells": [[5, 5]], "count": 3 }
```

---

#### `sequence` — Последовательность ходов

**Мощная система проверки паттернов нажатий.**

| Параметр | Описание |
|----------|----------|
| `moves` | Последовательность (`"u d l r"` или `"up down left right"`) |
| `mode` | Режим проверки (см. таблицу) |
| `count` | Количество вхождений |
| `operator` | `==`, `>=`, `<=`, `>`, `<`, `!=` для count |
| `min` / `max` | Диапазон вхождений |
| `overlapping` | Разрешить перекрывающиеся вхождения |
| `any` | Массив последовательностей (достаточно одной) |
| `all` | Массив последовательностей (нужны все) |

**Режимы (`mode`):**

| Режим | Символ | Описание |
|-------|--------|----------|
| `contains` | ∋ | Путь должен **содержать** последовательность (по умолчанию) |
| `not_contains` | ∌ | Путь **НЕ должен** содержать последовательность |
| `exact` | ≡ | Путь должен быть **ровно** такой последовательностью |
| `starts_with` | ⇒ | Путь должен **начинаться** с последовательности |
| `ends_with` | ⇐ | Путь должен **заканчиваться** последовательностью |

**Примеры:**

```json
// Путь должен содержать ↑↓
{ "check": "sequence", "moves": "u d" }

// Путь НЕ должен содержать два подряд хода вверх
{ "check": "sequence", "moves": "u u", "mode": "not_contains" }

// Путь должен быть ровно таким
{ "check": "sequence", "moves": "u u d d l r", "mode": "exact" }

// Начать с хода вправо
{ "check": "sequence", "moves": "r", "mode": "starts_with" }

// Закончить ходом влево
{ "check": "sequence", "moves": "l", "mode": "ends_with" }

// Содержать ровно 3 вхождения паттерна ↑↓
{ "check": "sequence", "moves": "u d", "count": 3, "operator": "==" }

// Содержать от 2 до 5 вхождений
{ "check": "sequence", "moves": "l r", "min": 2, "max": 5 }

// Не больше 2 разворотов (↑↓ или ↓↑)
{ "check": "sequence", "any": ["u d", "d u"], "max": 2 }

// Запретить несколько паттернов
{ "check": "sequence", "all": ["u u u", "d d d"], "mode": "not_contains" }

// Путь должен содержать ОБА паттерна
{ "check": "sequence", "all": ["u d", "l r"], "mode": "contains" }
```

---

#### `group` — Логические группы

Объединяйте условия через логику:

| Логика | Описание |
|--------|----------|
| `AND` | Все условия должны быть истинны (по умолчанию) |
| `OR` | Хотя бы одно условие истинно |
| `NOT` | Инвертировать первое условие |
| `XOR` | Ровно одно условие истинно |

```json
{
  "check": "group",
  "logic": "OR",
  "items": [
    { "check": "end_at", "cells": [[0, 0]] },
    { "check": "end_at", "cells": [[9, 9]] }
  ]
}
```

```json
// Сложное условие: (A AND B) OR C
{
  "check": "group",
  "logic": "OR",
  "items": [
    {
      "check": "group",
      "logic": "AND",
      "items": [
        { "check": "visit", "cells": [[1, 1]] },
        { "check": "visit", "cells": [[2, 2]] }
      ]
    },
    { "check": "visit", "cells": [[5, 5]], "count": 3, "operator": ">=" }
  ]
}
```

---

#### Специальные значения `cells`

Вместо списка координат можно использовать:

| Значение | Описание |
|----------|----------|
| `"corners"` | 4 угла карты |
| `"edges"` | Все клетки периметра |
| `"center"` | Центральная клетка |

```json
{ "check": "visit", "cells": "corners" }
{ "check": "end_at", "cells": "center" }
```

---

## ✏️ Режим Редактирования

Идеален для создания уровней без перезапуска игры.

### Как использовать

1. Запустите игру в **режиме 3**
2. Откройте `edit_user_level.json` в любом редакторе
3. Измените уровень
4. Сохраните файл
5. Нажмите **Enter** в игре
6. Уровень мгновенно перезагрузится!

### Горячие клавиши редактора

| Клавиша | Действие |
|---------|----------|
| `Enter` | Перезагрузить уровень из файла |
| `R` | Сбросить позицию |
| `Z` | Отменить ход |
| `X` | Показать/скрыть требования |
| `F9+F11` | Активировать dev-режим |

> Если файла `edit_user_level.json` нет, игра создаст его автоматически с примерами.

---

## ⚙️ Режим Разработчика

Нажмите `F9` + `F11` во время игры. В консоли появятся команды:

| Команда | Описание |
|---------|----------|
| `1` | Показать записанный путь (для sequence уровней) |
| `2` | Очистить запись пути |
| `3` | Показать координаты на клетках |
| `4` | Показать номера шагов на каждой клетке |
| `5` | Отключить победу (для тестирования) |
| `help` | Список команд |

---

## ❓ FAQ и Решение проблем

### Игрок умирает на границе карты
**Это нормально**, если там есть яд с типом `inner`. Яд срабатывает при попытке выйти из клетки, даже если цель — за пределами карты.

### Текст требований обрезается
Текст автоматически переносится. Если панель слишком узкая, уменьшите размер сетки или используйте более короткие формулировки.

### Коробки не двигаются
Проверьте:
- Нет ли `blocked` для нужной стороны
- Нет ли стены или другой коробки на пути
- Есть ли место для сдвига (не выходит за границы)

### Условие sequence не работает
- Используйте пробелы между ходами: `"u d l r"`, не `"udlr"`
- Проверьте режим: `contains` ищет подстроку, `exact` требует полное совпадение

### Уровень не загружается в режиме редактора
Проверьте:
1. Валидность JSON (запятые, скобки)
2. Наличие обязательного поля `start`
3. Консоль покажет ошибку парсинга

---

## 📝 Шаблон уровня

```json
{
  "name": "Мой уровень",
  "grid": [10, 10],
  "start": [0, 0],
  "hint": "Подсказка для игрока",
  
  "walls": [
    { "range": [[0, 0], [9, 0]], "sides": "u", "type": "both" }
  ],
  
  "poison": [
    { "cell": [5, 5], "sides": "all", "type": "both" }
  ],
  
  "movable": [
    { "cell": [3, 3] },
    { "range": [[6, 6], [7, 6]], "connected": true }
  ],
  
  "conditions": [
    { "check": "end_at", "cells": [[9, 9]] },
    { "check": "total_steps", "count": 30, "operator": "<=" }
  ]
}
```

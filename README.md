# 📖 Grid Puzzle Game - Руководство

## 📑 Оглавление

- [🎮 О игре](#-о-игре)
- [⌨️ Управление](#️-управление)
- [📊 Визуальные обозначения](#-визуальные-обозначения)
- [🎯 Типы уровней](#-типы-уровней)
- [📝 Создание уровней](#-создание-уровней)
  - [Базовая структура](#базовая-структура)
  - [Система барьеров](#система-барьеров)
  - [Все типы условий](#все-типы-условий)
- [📚 Примеры уровней](#-примеры-уровней)
- [🛠️ Режим разработчика](#️-режим-разработчика)
- [💡 Советы](#-советы)

---

## 🎮 О игре

Grid Puzzle Game — логическая головоломка на сетке. Управляйте белым кругом, выполняйте условия, избегайте ловушек.

**Два типа уровней:**
- **Sequence** — повторите точную последовательность ходов
- **Condition** — выполните все условия (посетите клетки, избегайте других, уложитесь в лимит шагов)

## ⌨️ Управление

| Клавиша | Действие |
|---------|----------|
| `↑ ↓ ← →` | Движение |
| `R` | Перезапуск уровня |
| `X` | Показать/скрыть требования |
| `F9 + F11` | Режим разработчика |

> 💡 Требования автоматически скрываются после первого хода. Нажмите `X` чтобы вернуть.

## 📊 Визуальные обозначения

### Иконки на клетках

| Символ | Значение |
|--------|----------|
| **✕** (красный) | Избегать эту клетку |
| **◎** (зелёный) | Финиш — закончить здесь |
| **•** (жёлтый) | Обязательно посетить |
| **→1, →2...** | Порядок посещения |
| **@N** | Первый визит на шаге N |
| **$N** | Последний визит на шаге N |
| **<N / ≥N** | Достичь до/после шага N |
| **x=N** | Посетить ровно N раз |
| **⟳N** | Остаться N ходов подряд |
| **∞** | Исключение из правила "без повторов" |
| **L=N** | Длина пути до клетки |

### Глобальные требования (правый верхний угол)

| Текст | Значение |
|-------|----------|
| **Шагов: =N / ≤N / ≥N** | Лимит на количество шагов |
| **Без повторов** | Каждую клетку — максимум 1 раз |

### Цвета барьеров

| Цвет | Тип |
|------|-----|
| 🟩 Зелёные линии | Стены — блокируют проход |
| 🔴 Красные линии | Яд — убивают при касании |

## 🎯 Типы уровней

### Sequence (Последовательность)
```json
{
  "type": "sequence",
  "start": [0, 0],
  "ans": "r r d d l l"
}
```
Серая клетка показывает финиш. Выполните точную последовательность.

### Condition (Условия)
```json
{
  "type": "condition",
  "start": [0, 0],
  "conditions": [...]
}
```
Выполните все условия из массива `conditions`.

## 📝 Создание уровней

### Базовая структура

```json
{
  "name": "Название",
  "type": "condition",
  "grid": [8, 6],
  "start": [0, 0],
  "hint": "Подсказка для игрока",
  "walls": [],
  "poison": [],
  "wall_is_poison": false,
  "conditions": []
}
```

| Параметр | Описание |
|----------|----------|
| `name` | Название уровня |
| `type` | `"sequence"` или `"condition"` |
| `grid` | Размер сетки `[ширина, высота]`. По умолчанию: `[16, 12]` |
| `start` | Стартовая позиция `[x, y]` |
| `hint` | Подсказка (показывается при включенных хинтах) |
| `walls` | Зелёные барьеры |
| `poison` | Красные барьеры (смертельные) |
| `wall_is_poison` | `true` — все стены становятся ядом. Или `{"except": [[x,y]]}` — с исключениями |
| `conditions` | Массив условий для типа `condition` |
| `ans` | Ответ для типа `sequence`: `"r r d d l l u u"` |

### Система барьеров

Барьеры размещаются на границах клеток.

**Формат 1 (простой):**
```json
"walls": [
  [[2, 3], "down", "both"]
]
```

**Формат 2 (компактный) — несколько сторон сразу:**
```json
"walls": [
  [[2, 3], {"down": "both", "right": "inner"}]
]
```

**Стороны:** `"up"`, `"down"`, `"left"`, `"right"`

**Типы:**
| Тип | Описание |
|-----|----------|
| `"inner"` | Блокирует **выход** из клетки |
| `"outer"` | Блокирует **вход** в клетку |
| `"both"` | Блокирует в обе стороны |

### Все типы условий

#### Основные

| check | Описание | Пример |
|-------|----------|--------|
| `end_at` | Закончить на клетке | `{"check": "end_at", "cells": [[7, 5]]}` |
| `visit_cells` | Посетить клетки | `{"check": "visit_cells", "cells": [[1,1], [2,2]], "match": "all"}` |
| `avoid_cells` | Избегать клетки | `{"check": "avoid_cells", "cells": [[3, 3]]}` |
| `total_steps` | Лимит шагов | `{"check": "total_steps", "count": 15, "operator": "<="}` |

#### Порядок и время

| check | Описание | Пример |
|-------|----------|--------|
| `visit_order` | Посетить в порядке | `{"check": "visit_order", "cells": [[0,0], [5,0], [5,5]]}` |
| `first_visit_at_step` | Первый визит на шаге N | `{"check": "first_visit_at_step", "cells": [[2,2]], "step": 5}` |
| `last_visit_at_step` | Последний визит на шаге N | `{"check": "last_visit_at_step", "cells": [[2,2]], "step": 10}` |
| `reach_before_step` | Достичь до шага N | `{"check": "reach_before_step", "cells": [[3,3]], "step": 5}` |
| `reach_after_step` | Достичь после шага N | `{"check": "reach_after_step", "cells": [[3,3]], "step": 10}` |
| `cell_has_steps` | Быть на клетке в конкретные шаги | `{"check": "cell_has_steps", "cells": [[2,2]], "required_steps": [0, 5, 10]}` |

#### Количество посещений

| check | Описание | Пример |
|-------|----------|--------|
| `visit_count` | Посетить N раз | `{"check": "visit_count", "cells": [[1,1]], "count": 3, "operator": "=="}` |
| `consecutive_visits` | Подряд N раз | `{"check": "consecutive_visits", "cells": [[2,2]], "count": 3}` |
| `no_revisit` | Без повторных посещений | `{"check": "no_revisit", "except": [[0,0]]}` |
| `path_length_to_cell` | Длина пути до клетки | `{"check": "path_length_to_cell", "cells": [[5,5]], "count": 10}` |

#### Специальные значения для `cells`

```json
"cells": "corners"   // Все 4 угла
"cells": "edges"     // Все клетки по периметру
"cells": "center"    // Центральная клетка
```

#### Параметр `match`

- `"any"` — хотя бы одна клетка (по умолчанию)
- `"all"` — все клетки из списка

#### Операторы `operator`

`"=="`, `">="`, `"<="`, `">"`, `"<"`, `"!="`

### Логические группы

Объединяйте условия логикой:

```json
{
  "check": "group",
  "logic": "OR",
  "items": [
    {"check": "end_at", "cells": [[0, 5]]},
    {"check": "end_at", "cells": [[7, 5]]}
  ]
}
```

**Доступные операторы:** `AND`, `OR`, `NOT`, `XOR`, `NAND`, `NOR`

## 📚 Примеры уровней
можно найти по ссылке: https://github.com/Pikatyu8/my-lil-puzzle-game/blob/main/levels.json

## 🛠️ Режим разработчика

**Активация:** `F9 + F11` одновременно

| Команда | Описание |
|---------|----------|
| `1` | Показать записанные ходы |
| `2` | Очистить запись |
| `3` | Вкл/выкл координаты на сетке |
| `4` или `cells` | Статистика посещений клеток |
| `5` | Вкл/выкл блокировку победы (для тестирования) |
| `help` | Меню команд |

## 💡 Советы по созданию уровней

1. **Начинайте просто** — 1-2 условия, маленькая сетка
2. **Тестируйте сами** — используйте режим разработчика
3. **Комбинируйте условия** — `visit_order` + `total_steps` = интересная задача
4. **Используйте барьеры** — они создают лабиринты
5. **wall_is_poison** — превращает безопасный лабиринт в опасный
6. **Группы с OR** — несколько способов победить

---

## 🐛 Проблемы?

| Проблема | Решение |
|----------|---------|
| Уровень не загружается | Проверьте JSON (запятые, скобки) |
| Не вижу требования | Нажмите `X` |
| Условие не срабатывает | Проверьте `match: "all"` vs `"any"` |
| Застрял | `R` для перезапуска |

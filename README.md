

# Руководство

## 📑 Оглавление

- [🚀 Запуск и загрузка уровней](#-запуск-и-загрузка-уровней)
- [🎮 О игре](#-о-игре)
- [⌨️ Управление](#️-управление)
- [📊 Визуальные обозначения](#-визуальные-обозначения)
- [🎯 Типы уровней](#-типы-уровней)
- [📝 Создание уровней](#-создание-уровней)
  - [Базовая структура](#базовая-структура-json)
  - [Система барьеров](#система-барьеров)
  - [Все типы условий](#все-типы-условий)
  - [Параметры шагов и групп](#параметры-шагов-и-групп)
- [🛠️ Режим разработчика](#️-режим-разработчика)

---

## 🚀 Запуск и загрузка уровней

При запуске игры открывается консольное окно, где вам будет предложено:

1.  **Загрузить свои уровни:** Введите `y` или `yes`, если у вас есть файл `user_levels.json` в папке с игрой. Если нажать Enter или ввести другое значение, загрузятся стандартные уровни (`levels.json`).
2.  **Включить подсказки:** Введите `y`, чтобы видеть текст из поля `"hint"` в консоли при старте уровня.
3.  **Выбрать уровень:** Введите номер уровня, чтобы начать сразу с него, или нажмите Enter для начала с первого.

> 💡 **Файл уровней:** Игра ищет `user_levels.json` рядом с исполняемым файлом. Используйте его для создания своих задач.

---

## 🎮 О игре

Grid Puzzle Game — логическая головоломка на сетке. Управляйте белым кругом, выполняйте условия, избегайте ловушек.

**Два типа уровней:**
- **Sequence** — повторите точную последовательность ходов (например, "вправо, вправо, вниз").
- **Condition** — выполните логические задачи (посетите клетки в определенное время, избегайте других, уложитесь в лимит шагов).

---

## ⌨️ Управление

| Клавиша | Действие |
|---------|----------|
| `↑ ↓ ← →` | Движение |
| `R` | Перезапуск уровня |
| `X` | Показать/скрыть требования на сетке |
| `F9 + F11` | Активация режима разработчика (в консоли) |

> 💡 Требования на клетках автоматически скрываются после первого хода, чтобы не мешать обзору. Нажмите `X`, чтобы вернуть их.

---

## 📊 Визуальные обозначения

### Иконки на клетках

| Символ | Цвет | Значение |
|--------|------|----------|
| **✕** | Красный | **Запрет.** Нельзя наступать на эту клетку. |
| **◎** | Зелёный | **Финиш.** Нужно закончить уровень здесь. |
| **•** | Жёлтый | **Посетить.** Обязательно пройти через эту клетку. |
| **→1, →2...** | Голубой | **Порядок.** Посетить клетки строго в указанной очередности. |
| **@N** | Голубой | **Первый визит.** Впервые ступить сюда строго на шаге N. |
| **$N** | Голубой | **Последний визит.** Последний раз ступить сюда на шаге N. |
| **<N / ≥N** | Голубой | **Тайминг.** Достичь до (<) или после (≥) шага N. |
| **x=N** | Жёлтый | **Счетчик.** Посетить ровно N раз (или ≥N, ≤N). |
| **⟳N** | Жёлтый | **Задержка.** "Топтаться" на месте (посетить подряд) N раз. |
| **∞** | Жёлтый | **Исключение.** Клетка, в которую можно возвращаться (если включен глобальный запрет повторов). |
| **L=N** | Голубой | **Длина пути.** Путь до первого посещения этой клетки должен быть равен N. |
| **⊘N** | Розовый | **Временный запрет.** Нельзя находиться здесь на шаге N (или в диапазоне шагов). |
| **✓N** | Зелёный | **Временное требование.** Обязательно быть здесь на шаге N. |
| **• + ⊘** | Жёлтый+Розовый | **Гибрид.** Обязательно посетить, НО не в запрещенные шаги. |

### Глобальные требования (правый верхний угол)

| Текст | Значение |
|-------|----------|
| **Шагов: =N / ≤N** | Общий лимит ходов на уровень. |
| **Без повторов** | Глобальное правило: в каждую клетку можно зайти только 1 раз. |

### Барьеры

| Вид | Тип | Описание |
|-----|-----|----------|
| 🟩 Зелёные линии | **Стены** | Блокируют проход. |
| 🔴 Красные линии | **Яд** | Мгновенная смерть при касании. |

---

## 🎯 Типы уровней

### Sequence (Последовательность)
Игрок не знает маршрут, он должен угадать его или найти методом проб и ошибок.
```json
{
  "type": "sequence",
  "start": [0, 0],
  "ans": "r r d d l l"
}
```
Серая клетка показывает, где окажется игрок в конце правильной последовательности.

### Condition (Условия)
Открытая информация. Игрок видит иконки и должен спланировать маршрут, удовлетворяющий всем требованиям.
```json
{
  "type": "condition",
  "start": [0, 0],
  "conditions": [...]
}
```

---

## 📝 Создание уровней

### Базовая структура JSON

```json
{
  "name": "Название уровня",
  "type": "condition", 
  "grid": [16, 12],      // Ширина, Высота (по умолчанию 16x12)
  "start": [0, 0],       // Старт [x, y]
  "hint": "Текст подсказки",
  "walls": [],
  "poison": [],
  "wall_is_poison": false, // Если true, все walls становятся poison
  "conditions": []       // Только для type="condition"
}
```

### Система барьеров

Барьеры описываются координатами клетки и стороной, где стоит препятствие.

**Синтаксис:**
```json
"walls": [
  // Простой формат: [[x, y], "сторона", "тип"]
  [[2, 3], "down", "both"],
  
  // Групповой формат: [[x, y], { "сторона": "тип", "сторона": "тип" }]
  [[5, 5], {"up": "inner", "right": "outer"}]
]
```

*   **Стороны:** `"up"`, `"down"`, `"left"`, `"right"`
*   **Типы:**
    *   `"inner"` — нельзя **выйти** из клетки в эту сторону.
    *   `"outer"` — нельзя **войти** в клетку с этой стороны.
    *   `"both"` — полная стена.

### Все типы условий

Условия добавляются в массив `"conditions"`.

#### 1. Основные цели
| `check` | Описание | Пример |
|---------|----------|--------|
| `end_at` | Закончить уровень на указанной клетке. | `{"check": "end_at", "cells": [[7, 5]]}` |
| `visit_cells` | Посетить клетки (порядок не важен). | `{"check": "visit_cells", "cells": [[1,1], [2,2]]}` |
| `avoid_cells` | Никогда не заходить в клетки. | `{"check": "avoid_cells", "cells": [[3, 3]]}` |
| `total_steps` | Лимит шагов. | `{"check": "total_steps", "count": 10, "operator": "<="}` |

#### 2. Порядок и Тайминги
| `check` | Описание | Пример |
|---------|----------|--------|
| `visit_order` | Посетить список клеток строго по порядку. | `{"check": "visit_order", "cells": [[0,0], [5,0]]}` |
| `first_visit_at_step` | Первый вход в клетку должен быть на шаге N. | `{"check": "first_visit_at_step", "step": 5, "cells": [[2,2]]}` |
| `last_visit_at_step` | Последний вход (или нахождение) на шаге N. | `{"check": "last_visit_at_step", "step": 10, "cells": [[2,2]]}` |
| `reach_before_step` | Посетить клетку **до** шага N (строго <). | `{"check": "reach_before_step", "step": 5, "cells": [[3,3]]}` |
| `reach_after_step` | Посетить клетку **после или на** шаге N (≥). | `{"check": "reach_after_step", "step": 10, "cells": [[3,3]]}` |
| `path_length_to_cell` | Длина пути до первого попадания в клетку равна N. | `{"check": "path_length_to_cell", "count": 10, "cells": [[5,5]]}` |

#### 3. Сложные временные ограничения
| `check` | Описание | Пример |
|---------|----------|--------|
| `cell_has_steps` | Клетка должна содержать в истории конкретные шаги (например, быть там на 0-м и 5-м ходу). | `{"check": "cell_has_steps", "cells": [[2,2]], "required_steps": [0, 5]}` |
| `avoid_at_steps` | **Временный запрет:** нельзя находиться в клетке на указанных шагах. | `{"check": "avoid_at_steps", "cells": [[2,2]], "step_range": [2, 5]}` |
| `require_at_steps` | **Временное требование:** игрок обязан быть в одной из клеток на указанном шаге. | `{"check": "require_at_steps", "cells": [[2,2]], "step": 3}` |
| `visit_except_at_steps` | **Гибрид:** Обязательно посетить клетку, но ЗАПРЕЩЕНО быть в ней на указанных шагах. | `{"check": "visit_except_at_steps", "cells": [[1,1]], "step": 5}` |

> ⚠️ **Важно про `visit_except_at_steps`:** По умолчанию параметр `match` равен `"all"` (нужно посетить все указанные клетки безопасно).

#### 4. Счетчики и Повторы
| `check` | Описание | Пример |
|---------|----------|--------|
| `visit_count` | Посетить клетку N раз. | `{"check": "visit_count", "count": 3, "operator": ">=", "cells": [[1,1]]}` |
| `consecutive_visits` | Оставаться в клетке N ходов подряд (топтаться). | `{"check": "consecutive_visits", "count": 3, "cells": [[2,2]]}` |
| `no_revisit` | Запрет на повторное посещение любых клеток. Можно задать исключения. | `{"check": "no_revisit", "except": [[0,0]]}` |

---

### Параметры шагов и групп

#### Указание шагов (для условий `*_at_steps`)
*   `"step": 5` — конкретный шаг.
*   `"step_range": [2, 5]` — диапазон от 2 до 5 включительно.
*   `"steps": [1, 3, 5]` — список конкретных шагов.

#### Параметр `match`
Определяет, как проверять список `cells`:
*   `"any"` (по умолчанию для большинства) — достаточно выполнить условие для **одной** любой клетки из списка.
*   `"all"` (по умолчанию для `visit_except_at_steps`) — нужно выполнить для **всех** клеток.

#### Группировка условий
Можно объединять условия логическими операторами:
```json
{
  "check": "group",
  "logic": "OR",  // AND, OR, NOT, XOR, NAND, NOR
  "items": [
    {"check": "end_at", "cells": [[0, 5]]},
    {"check": "end_at", "cells": [[7, 5]]}
  ]
}
```

#### Специальные значения для `cells`
Вместо координат можно писать строки:
*   `"corners"` — 4 угла.
*   `"edges"` — периметр.
*   `"center"` — центр поля.

---

## 🛠️ Режим разработчика

Позволяет отлаживать уровни.
**Как включить:** Нажмите `F9` + `F11` во время игры. В консоли появится сообщение о разблокировке.

**Команды консоли:**
| Команда | Описание |
|---------|----------|
| `1` | Вывести текущую последовательность ходов (для создания Sequence уровней). |
| `2` | Очистить запись ходов. |
| `3` | Вкл/выкл отображение координат на клетках. |
| `4` или `cells` | Вывести статистику посещения каждой клетки (какие шаги были сделаны). |
| `5` | Отключить победу (позволяет ходить после выполнения условий). |
| `help` | Показать меню команд. |
